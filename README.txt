***Para correr el backend:
cd demoback
./gradlew bootRun

***Para detener el backend(cuando haya cambios):
(Ctrl + C)
# Y vuelve a ejecutar

***Para correr el front:
cd inicoangular
npm install
ng serve

***Para h2
http://localhost:8080/h2-console
JDBC URL: jdbc:h2:file:./h2/systemvisitas-db
2 veces sa

***Para los cambios en github
git add .
git commit -m "Actualizando Proyecto"
git push